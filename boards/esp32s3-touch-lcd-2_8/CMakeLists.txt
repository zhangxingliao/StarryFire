# Board component: provides sdkconfig.defaults (chipset/RF/driver/memory/partitions) +
# Kconfig.projbuild (hidden pin and font-size symbols) and partitions.csv; no source files.
idf_component_register()
